import React from 'react';
import { Card, Form } from 'antd';

const WeChat = () => {
  return (
    <Card title="测试标题">
      <Form layout="inline"></Form>
    </Card>
  );
};

export default WeChat;
