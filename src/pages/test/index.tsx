import React from 'react';
import { Card, Form } from 'antd';

const IndexPage = () => {
  return (
    <Card title="测试标题">
      <Form layout="inline" />
    </Card>
  );
};

export default IndexPage;
